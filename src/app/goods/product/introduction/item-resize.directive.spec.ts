import { ItemResizeDirective } from './item-resize.directive';

describe('ItemResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemResizeDirective();
    expect(directive).toBeTruthy();
  });
});
