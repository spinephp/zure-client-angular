import { PopbuttonDirective } from './popbutton.directive';

describe('PopbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new PopbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
